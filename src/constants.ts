import { ToolbarState } from './core/types';

export const defaultStyles: ToolbarState = {
	textAlign: 'left',
	fontWeight: 'normal',
	fontStyle: 'normal',
	textDecoration: 'none'
};

export const defaultTitle = 'Новая таблица';
